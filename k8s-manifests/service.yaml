apiVersion: v1
kind: Service
metadata:
  name: task-tracker-service
spec:
  type: NodePort
  selector:
    app: task-tracker
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 30080